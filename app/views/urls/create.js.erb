$('.alert-success').removeClass('hidden');
$('.alert-success .alert-link')
  .attr('href', '<%= @url.shorten_url.link(request.base_url) %>')
  .html('<%= @url.shorten_url.link(request.base_url) %>');
