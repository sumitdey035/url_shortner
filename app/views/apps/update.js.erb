<% unless @app.errors.empty? %>
  <% @app.errors.full_messages.each do |message| %>
    US.Utility.toastError('<%= message %>')
  <% end %>
<% else %>
  US.Utility.toastSuccess('The app has been updated successfully')
  $('.edit_app').replaceWith('<%= j render @app %>')
<% end %>